@model IEnumerable<MVCTagHelper.Models.Afdeling>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AfdelingNaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Locatie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AfdelingNaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Locatie.LocatieId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AfdelingId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AfdelingId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AfdelingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
